//
// Created by Chengwei Zhang on 7/17/20.
//

#include <iostream>
#include <cstdio>
#include <cmath>
#include <climits>
#include <algorithm>
#include <vector>
#include <queue>
#include <climits>

using namespace std;

int main() {
    int c, n;
    sacnf("%d%d", &c, &n);
    int* prices = new int[n];
    int* values = new int[n];
    for (int i = 0; i < n; i ++) {
        scanf("%d%d", &prices[i], &value[i]);
    }
    
}